# ============================================================
# Digital FTE — Environment Variables Template
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# REQUIRED = must have at least one to use the app
# OPTIONAL = enhances features but not strictly needed
# ============================================================

# ============================================================
# LLM PROVIDERS  (REQUIRED — need at least one)
# ============================================================
# The app tries each in order: OpenAI → Gemini → Groq
# Get one of these to get started (Groq is free):

# OpenAI — https://platform.openai.com/api-keys
# Best quality. GPT-4o is the primary model.
OPENAI_API_KEY=your_openai_api_key

# Google Gemini — https://aistudio.google.com/app/apikey
# Free tier available. Gemini 2.5 Flash is fast and capable.
GOOGLE_AI_API_KEY=your_gemini_api_key

# Groq — https://console.groq.com/keys
# Free tier with generous limits. Llama 3.3 70B.
GROQ_API_KEY=your_groq_api_key

# ============================================================
# JOB SEARCH  (REQUIRED — need at least one)
# ============================================================

# SerpAPI — https://serpapi.com/manage-api-key
# 100 free searches/month. Best Google Jobs results.
SERPAPI_API_KEY=your_serpapi_key

# RapidAPI / JSearch — https://rapidapi.com/letscrape-6bRBa3QguO5/api/jsearch
# 200 free requests/month (JSearch plan).
RAPIDAPI_KEY=your_rapidapi_key

# ============================================================
# HR CONTACT FINDING  (OPTIONAL — add at least one for email outreach)
# ============================================================
# At least one gives the email-outreach pipeline HR contacts.
# All have generous free tiers.

# Apify — https://console.apify.com/account/integrations
# Free $5 credit on signup. LinkedIn people search + contact scraper.
APIFY_API_KEY=your_apify_key

# Prospeo — https://app.prospeo.io/api-key
# 150 free domain email searches/month.
PROSPEO_API_KEY=your_prospeo_key

# Hunter.io — https://hunter.io/api-keys
# 25 free domain email searches/month. Very reliable.
HUNTER_API_KEY=your_hunter_key

# Snov.io — https://app.snov.io/api-setting
# 50 free searches/month.
SNOV_CLIENT_ID=your_snov_client_id
SNOV_CLIENT_SECRET=your_snov_secret

# ============================================================
# GOOGLE CLOUD  (OPTIONAL — for Gmail email sending)
# ============================================================
# Needed only if you want the app to send emails via your Gmail.
# See SETUP.md → "Google OAuth / Gmail" section for full steps.

GOOGLE_CLOUD_PROJECT_ID=your_project_id
GOOGLE_OAUTH_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=GOCSPX-your_client_secret
GOOGLE_REFRESH_TOKEN=your_refresh_token
GOOGLE_CREDENTIALS_PATH=./credentials.json

# ============================================================
# DATABASE  (OPTIONAL — defaults to local SQLite)
# ============================================================
# Leave blank to use SQLite (./digital_fte.db) — perfect for local dev.
# Fill in Supabase values for production / multi-user deployments.
# See SETUP.md → "Supabase (Production DB)" section.

SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
DATABASE_URL=postgresql+asyncpg://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
DIRECT_DATABASE_URL=postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres

# ============================================================
# UPSTASH REDIS  (OPTIONAL — for API quota caching)
# ============================================================
# Free tier at https://console.upstash.com
# Leave blank to skip Redis (quota tracking will be in-memory only).

UPSTASH_REDIS_URL=rediss://default:<password>@<endpoint>.upstash.io:6379
UPSTASH_REDIS_REST_URL=https://<endpoint>.upstash.io
UPSTASH_REDIS_REST_TOKEN=AXxxxxxxxxxxxxxxxxxxxx

# ============================================================
# OBSERVABILITY  (OPTIONAL — LLM call tracing)
# ============================================================

# LangSmith — https://smith.langchain.com (free tier)
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=your_langsmith_key
LANGCHAIN_PROJECT=digital-fte

# Langfuse — https://cloud.langfuse.com (free tier)
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key
LANGFUSE_SECRET_KEY=your_langfuse_secret_key
LANGFUSE_HOST=https://cloud.langfuse.com

# ============================================================
# APP CONFIGURATION
# ============================================================

# JWT secret — CHANGE THIS! Generate with: openssl rand -hex 32
SECRET_KEY=change-me-generate-with-openssl-rand-hex-32

# Frontend URL — comma-separated list of allowed CORS origins (for production)
# Example: FRONTEND_URL=https://your-app.vercel.app
FRONTEND_URL=

# Environment: development | production
APP_ENV=development

# Enable debug logging
DEBUG=true
LOG_LEVEL=DEBUG

# File upload directories (relative to backend/)
UPLOAD_DIR=./uploads
GENERATED_DIR=./generated
